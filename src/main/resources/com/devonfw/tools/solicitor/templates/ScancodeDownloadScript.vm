SET beginning=%CD%
mkdir scancodeOutput
cd SourceDownload
SET dir=%CD%

cd ../../../scancode/scancode-toolkit-21.3.31
#foreach ($artifact in $ARTIFACTS)
		call scancode -c -n 16 --json-pp %beginning%/scancodeOutput/$artifact.artifactId-$artifact.version).json --only-findings %dir%/$artifact.artifactId-$artifact.version
#end

cd %beginning%/scancodeOutput
copy *.json scancodeOutput.json 

move scancodeOutput.json %beginning%