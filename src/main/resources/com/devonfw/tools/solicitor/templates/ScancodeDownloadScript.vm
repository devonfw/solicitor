SET beginning=%CD%
mkdir scancodeOutput
cd SourceDownload
SET dir=%CD%

cd ../../../scancode/scancode-toolkit-21.3.31
#foreach ($artifact in $ARTIFACTS)

SET art=$artifact.artifactId

#[[SET cutArt=%art:/=+%]]#
		call scancode -c -n 16 --json-pp %beginning%/scancodeOutput/%cutArt%-$artifact.version).json --only-findings %dir%/%cutArt%-$artifact.version
#end

cd %beginning%/scancodeOutput
copy *.json scancodeOutput.json 

move scancodeOutput.json %beginning%